<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
  <a href="/">Home</a>
    <h1>Welcome</h1>
    <h3>Below click add skill to add an additional skill. To delete a skill select the skill that you want to delete.</h3>
    <ul>
        <% skills.forEach(function(s) { %>
        <li>
            <a href="/skills/<%= s.id %>"><%= s.skill %></a>
            -
            <%= s.learned ? 'learned' : 'not learned' %>
        </li>
        <% }); %>
    </ul>
    <a href="/skills/new">Add Skill</a>
</body>
</html>